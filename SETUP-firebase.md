# 서적부 POS — Firebase 연결 안내

여러 사람이 같이 쓰려면 아래 1~6번을 **keonchue@gmail.com** 계정으로 직접 해주세요.
(약 10분) 다 하시면 화면에 나온 **config 값**과 **가게 계정 이메일**을 알려주시면
나머지 연결은 제가 처리합니다.

---

## 1. 프로젝트 만들기
1. https://console.firebase.google.com 접속 → 구글 로그인
2. **[프로젝트 만들기]**
3. 이름: 예) `seojeokbu-pos`
4. Google 애널리틱스는 꺼도 됨 → **만들기**

## 2. Firestore 데이터베이스 켜기
1. 왼쪽 메뉴 **빌드 → Firestore Database**
2. **[데이터베이스 만들기]**
3. 위치: **asia-northeast3 (서울)** 선택
4. 시작 모드: **프로덕션 모드** 선택 → 사용 설정
   (규칙은 나중에 우리가 만든 걸로 교체합니다)

## 3. 로그인(인증) 켜기
1. 왼쪽 메뉴 **빌드 → Authentication → [시작하기]**
2. **Sign-in method** 탭 → **이메일/비밀번호** → 사용 설정 → 저장

## 4. 가게 공용 계정 만들기
1. Authentication → **Users** 탭 → **[사용자 추가]**
2. 이메일: 예) `store@seojeokbu.com` (실제로 쓸 공용 아이디)
3. 비밀번호: 직원들과 공유할 비밀번호
4. 추가 → 이 **이메일**을 저에게 알려주세요 (보안 규칙에 넣습니다)

## 5. 웹 앱 등록하고 config 받기
1. 왼쪽 위 **⚙️ → 프로젝트 설정**
2. 아래 **내 앱** → **웹 아이콘(`</>`)** 클릭
3. 앱 닉네임: 예) `pos-web` → 등록 (Hosting 체크는 안 해도 됨)
4. 화면에 나오는 `const firebaseConfig = { ... }` 블록을 **그대로 복사해서** 저에게 주세요

## 6. (배포 후) 접속 도메인 허용
GitHub Pages로 올린 뒤 주소가 정해지면:
1. Authentication → **Settings → 승인된 도메인**
2. `<본인아이디>.github.io` 를 **추가**
   (이걸 안 하면 배포된 사이트에서 로그인이 막힙니다)

---

## 저에게 전달해 주실 것
- [ ] 5번의 `firebaseConfig { ... }` 값 전체
- [ ] 4번에서 만든 가게 계정 **이메일**

두 개만 주시면 `firebase-config.js` 채우고, 앱을 Firebase에 연결하고,
`firestore.rules` 보안 규칙을 적용하는 것까지 이어서 하겠습니다.
